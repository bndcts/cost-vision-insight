# FastAPI settings
CMS_ENVIRONMENT=development
CMS_API_V1_PREFIX=/api/v1

# Database connection string (override in docker-compose if needed)
CMS_DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/cost_model

# OpenAI credentials (provide your real key locally, never commit secrets)
CMS_OPENAI_API_KEY=sk-your-openai-key
CMS_OPENAI_MODEL=gpt-4o-mini
